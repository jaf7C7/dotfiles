#!/usr/bin/bash
#
# upper: Uppercases input text char-by-char.
#
# For use with `nano` for uppercasing the region.

main() {
	while IFS= read -N 1; do
		set -- "$@" "${REPLY^^}"
	done
	printf '%s' "$@"
}
main "$@"
