#!/bin/sh
#
# grepedit
#
# Like grep, but opens $EDITOR on each file at the matching line.
#
exec awk '
BEGIN {
	cmd = "grep --color=always"
	for (i = 1; i < ARGC; i++)
		cmd = cmd " " ARGV[i]
	while (cmd | getline > 0)
		print()
}
' "$@"
